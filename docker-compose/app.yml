services:
  app:
    build:
      context: ../app
      dockerfile: Dockerfile
      target: ${BUILD_TARGET:-development}
    container_name: argussbomguard_app
    image: argussbomguard:${APP_ENV:-development}
    expose:
      - "50051"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: on-failure
    logging:
      driver: local
      options:
        max-size: ${LOG_MAX_SIZE:-10m}
        max-file: ${LOG_MAX_FILE:-3}

  worker:
    build:
      context: ../app
      dockerfile: Dockerfile
      target: ${BUILD_TARGET:-development}
    image: argussbomguard:${APP_ENV:-development}
    user: appuser
    command: celery -A celery_app worker --loglevel=info --concurrency=4
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "celery", "-A", "celery_app", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: on-failure
    logging:
      driver: local
      options:
        max-size: ${LOG_MAX_SIZE:-10m}
        max-file: ${LOG_MAX_FILE:-3}

  scheduler:
    build:
      context: ../app
      dockerfile: Dockerfile
      target: ${BUILD_TARGET:-development}
    image: argussbomguard:${APP_ENV:-development}
    user: appuser
    command: celery -A celery_app beat --loglevel=info
    depends_on:
      - rabbitmq
    restart: on-failure
    logging:
      driver: local
      options:
        max-size: ${LOG_MAX_SIZE:-10m}
        max-file: ${LOG_MAX_FILE:-3}
